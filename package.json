{
  "name": "bytestash",
  "version": "1.0.0",
  "main": "electron/main.js",
  "scripts": {
    "start": "concurrently \"cd client && npm start\" \"cd server && npm start\"",
    "electron:dev": "cross-env NODE_ENV=development concurrently \"npm start\" \"wait-on http://localhost:3000 && electron .\"",
    "build": "cd client && npm run build && cd ../server && npm run build",
    "electron:build": "electron-builder",
    "web:build": "docker-compose up --build",
    "watch:tailwind": "tailwindcss -i src/index.css -o src/output.css --watch",
    "dev": "concurrently \"npm run watch:tailwind\" \"your-existing-electron-start-command\""
  },
  "dependencies": {
    "sqlite": "^5.1.1",
    "sqlite3": "^5.1.7"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "cross-env": "^7.0.3",
    "electron": "^28.0.0",
    "electron-builder": "^24.0.0",
    "wait-on": "^7.2.0"
  }
}
